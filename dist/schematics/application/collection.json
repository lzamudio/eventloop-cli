{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicApplication",
  "title": "Name of application",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of application",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the new project?"
    }
  },
  "required": ["name"]
}
